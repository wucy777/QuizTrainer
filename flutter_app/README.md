# QuizTrainer (Flutter)

通用选择题刷题工具的双端实现（Windows / Android）。

应用源码在 `lib/`，题库格式与构建说明见上级目录 `Document/`。

```bash
flutter run -d windows
flutter build windows --release
flutter build apk --release
flutter test
```
